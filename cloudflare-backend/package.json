{
  "name": "question-bank-cloudflare",
  "version": "1.0.0",
  "description": "Question Bank Quiz System for Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "db:migrate": "wrangler d1 execute question-bank-db --file=./schema.sql",
    "db:query": "wrangler d1 execute question-bank-db --command="
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.0.0"
  }
}